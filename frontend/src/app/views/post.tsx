import { formatDate, getStrapiMedia } from '@/app/lib/api-helpers';
import Image from 'next/image';

export default function Post(props: {}) {
  return (
    <article className="space-y-8 dark:bg-black dark:text-gray-50">
      Post
    </article>
  );
}
